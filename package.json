{
  "name": "otto",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "NODE_ENV=production cd frontend && npm run build",
    "postinstall": "cd frontend && npm install"
  }
}
